export const APP_CONFIG = Object.freeze({
  useFirebase: true,
  useMockData: false,
  locale: 'es-PE',
  currency: 'PEN',
  pageSize: 8,
  maxImageSize: 5 * 1024 * 1024,
  maxProductImages: 5,
  debug: true
});
